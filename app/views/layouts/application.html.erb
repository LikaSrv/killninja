<!DOCTYPE html>
<html>
  <head>
    <title>Killninja</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
  </head>
  <body>
    <nav class="navbar navbar-light bg-light justify-content-between">
      <a class="navbar-brand" href="<%=ninjas_path%>">
        <%= image_tag("logo.png", width: 50 ) %>
      </a>
      <form class="form-inline row my-é my-lg-0">
        <div class="d-flex" style="align-items: baseline;">
          <a class="btn btn-outline-primary" href="<%= new_ninja_path%>">
            Mes ninjas
          </a>
          <input class="form-control mr-sm-2" type="search" placeholder="Rechercher" aria-label="Rechercher">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Rechercher</button>
          <ul class="navbar-nav d-flex flex-grow-& justify-content-center">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'Déconnecter', destroy_user_session_path, method: :delete, class: 'nav-link nav-end' %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to 'Connection', new_user_session_path, class: 'nav-link nav-end' %>
              </li>
            <% end %>
          </ul>
        </div>
      </form>
    </nav>
    <%= yield %>
    <%= render "layouts/flashes" %>
  </body>
</html>
