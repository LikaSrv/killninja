<div>
  <%= simple_form_for [reservation, review], style:"color: black" do |r| %>
    <%= r.input :score, label: "Note", collection: 1..5, as: :radio_buttons%>
    <%= r.input :content, label:"Commentaire", as: :text, style:"color: antiquewhite !important"%>
    <%= r.button :submit, "Créer le commentaire", class: "btn btn-primary" %>
  <% end %>
</div>
